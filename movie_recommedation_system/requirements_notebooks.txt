# Extra dependencies used by the Jupyter notebooks (not required to run `app.py`).
sentence-transformers
tqdm
jupyter
ipykernel

# Training / experimentation (Surprise SVD)
scikit-surprise==1.1.4
Cython<3.0
