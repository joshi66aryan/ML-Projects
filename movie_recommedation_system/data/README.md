# data/

This folder stores datasets used to train and run the recommender.

## Subfolders
- `ml-latest-small/`
  - The MovieLens dataset (ratings + movie metadata) used for collaborative filtering training.
- `processed/`
  - Cleaned/enriched outputs used by the Streamlit app (titles, genres, overviews, poster paths, etc.).

